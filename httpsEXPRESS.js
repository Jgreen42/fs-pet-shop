

// Protocol: HTTP 
// Version
// Headers
// Body

// Path
// Method

// http Response
// Version
// // Set Headers
// Body
// Status code 
// Const app = express();
// middlewareż(monitor with a pt belt) to parse information
// app.use(express.text()); 
// app.use(express.json());

// slash method gets rid of allot of code needed
// App.get(“/“ (req, res) => {    
//access headers
// Console.log(req.header(“user-Agent”)); // 1 method
//console.log(req.headers); // 2 method

//acesss path
// console.log(req.path);
//console.log(typeof req.body); // already been parsed through automated middleware to argument to be modified
//set headers
//res.set(‘content-type’, ‘text/plain’).status(201).send(“response”); // also setting test code



//Req.send(“response”);


//access body can’t parse it normally because it could be encoded in a certain way use middle ware to parse the information
//console.log(

//});

 //app.get(‘/pets’, (req, res) => {}); //  app.get(‘/pets/:index”, (req, res) => {});
//console.log(req.params.index);
//res.set(‘content-type’, ‘text/plain’).status(201).send(“response”); // also setting test code

 //app.listen(3000);




//When sending the audio file for example or some form of data is to send the type 
//of file that you are using so the user or client knows how to interpret the type of 
//files 
//they are accessing ect/ type of common file is (text/plain) 